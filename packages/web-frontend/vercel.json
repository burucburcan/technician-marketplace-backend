{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "installCommand": "cd ../.. && npm install --no-optional && npm install @rollup/rollup-linux-x64-gnu",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
